export interface ICard {
    cardId: string;
    cardNumber: string;
    cardExpiration: string;
    cardCVV: string;
    cardDescription: string;
    userEmail: string;
    createdAt: string;
    updatedAt: string;
}
